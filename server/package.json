{
  "name": "server",
  "version": "1.0.0",
  "description": "Full stack app server",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch server.js"
  },
  "author": "Ken Poon",
  "license": "MIT",
  "devDependencies": {
    "express": "^5.1.0"
  }
}
